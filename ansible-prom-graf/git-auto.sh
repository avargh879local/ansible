#!/bin/bash

# Navigate to your Git repository directory
# cd /path/to/your/repository

# Add all changes to staging
git add .

# Commit the changes with a message
git commit -m "hey"

# Push the changes to the master branch on origin
git push origin master

